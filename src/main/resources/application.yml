spring:
  security:
    user:
      name: user
      password: 1234
    oauth2:
      resourceserver:

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: user
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

dreamSecretKey2006: mySuperSecretKeyThatIsLongEnough123456!
jwt:
  expiration: 86400000
